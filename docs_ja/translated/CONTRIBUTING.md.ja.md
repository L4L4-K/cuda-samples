# CONTRIBUTING.md Japanese Companion

Original English document: [`CONTRIBUTING.md`](../../CONTRIBUTING.md)

## English Reference

This companion keeps every parsed English source paragraph and places Japanese study notes directly below it. Commands, paths, APIs, target names, output strings, LICENSE text, headers, and attribution remain in English.

> **日本語**
> この companion は、原文の各 paragraph を英語のまま残し、その直下に日本語の理解メモを追加します。実行名、API 名、path、target、出力文字列は翻訳しません。
> **学習メモ**
> 迷った場合は英語の原文を authoritative source とし、日本語は CUDA の前提、build/run の流れ、memory/sync/performance の読みどころを補う secondary material として使います。

## Paragraph Notes

## Source Paragraph 001

Context: `Contributing to the CUDA Samples`

English paragraph 1:
> Thank you for your interest in contributing to the CUDA Samples!

> **日本語**
> この段落は `Contributing to the CUDA Samples` セクションの contributor 向け手順です。主要な語句 を変えず、作業単位、検証、formatting の目的を確認します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 002

Context: `Getting Started`

English paragraph 2:
> 1. **Fork & Clone the Repository**:

> **日本語**
> 原文は contributor が fork と clone で作業 copy を用意する流れを説明しています。repository の ownership と remote の向きを確認するための項目です。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 003

Context: `Getting Started`

English paragraph 3:
> Fork the reporistory and clone the fork. For more information, check [GitHub's documentation on
> forking](https://docs.github.com/en/github/getting-started-with-github/fork-a-repo) and [cloning a
> repository](https://docs.github.com/en/github/creating-cloning-and-archiving-repositories/cloning-a-repository).

> **日本語**
> 原文は contributor が fork と clone で作業 copy を用意する流れを説明しています。repository の ownership と remote の向きを確認するための項目です。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 004

Context: `Making Changes`

English paragraph 4:
> 1. **Create a New Branch**:

> **日本語**
> 原文は変更を専用 branch に分ける手順を説明しています。local branch 名と作業範囲を分けると、sample 別の差分を追いやすくなります。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 005

Context: `Making Changes`

English paragraph 5:
> 2. **Make Changes**.

> **日本語**
> この段落は `Making Changes` セクションの contributor 向け手順です。`Make Changes` を変えず、作業単位、検証、formatting の目的を確認します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 006

Context: `Making Changes`

English paragraph 6:
> 3. **Build and Test**:

> **日本語**
> 原文は変更後に build と test で既存 behavior を壊していないか確認することを求めています。CUDA sample では toolchain と GPU availability も結果に影響します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 007

Context: `Making Changes`

English paragraph 7:
> Ensure changes don't break existing functionality by building and running tests.

> **日本語**
> 原文は変更後に build と test で既存 behavior を壊していないか確認することを求めています。CUDA sample では toolchain と GPU availability も結果に影響します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 008

Context: `Making Changes`

English paragraph 8:
> For more details on building and testing, refer to the [Building and Testing](#building-and-testing) section below.

> **日本語**
> 原文は変更後に build と test で既存 behavior を壊していないか確認することを求めています。CUDA sample では toolchain と GPU availability も結果に影響します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 009

Context: `Making Changes`

English paragraph 9:
> 4. **Commit Changes**:

> **日本語**
> 原文は変更を commit にまとめる手順です。小さな commit は docs、annotation、verification の境界を後から読み返しやすくします。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 010

Context: `Building and Testing`

English paragraph 10:
> For information on building a running tests on the samples, please refer to the main [README](README.md)

> **日本語**
> 原文は変更後に build と test で既存 behavior を壊していないか確認することを求めています。CUDA sample では toolchain と GPU availability も結果に影響します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 011

Context: `Creating a Pull Request`

English paragraph 11:
> 1. Push changes to your fork 2. Create a pull request targeting the `master` branch of the original CUDA Samples
> repository. Refer to [GitHub's
> documentation](https://docs.github.com/en/github/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/about-pull-requests)
> for more information on creating a pull request. 3. Describe the purpose and context of the changes in the pull
> request description.

> **日本語**
> 原文は upstream repository 向けの pull request 手順を説明しています。このローカル学習作業では PR は作成せず、文書上の参照としてだけ読みます。
> **学習メモ**
> この goal の hard rule では PR を作成しません。ここでは原文の意味を理解するだけで、実行手順として扱いません。

## Source Paragraph 012

Context: `Code Formatting (pre-commit hooks)`

English paragraph 12:
> The CUDA Samples repository uses [pre-commit](https://pre-commit.com/) to execute all code linters and formatters.
> These tools ensure a consistent coding style throughout the project. Using pre-commit ensures that linter versions and
> options are aligned for all developers. Additionally, there is a CI check in place to enforce that committed code
> follows our standards.

> **日本語**
> 原文は formatting と lint の自動チェックを説明しています。`pre-commit` は coding style をそろえる道具で、CUDA kernel の出力仕様とは別に扱います。
> **学習メモ**
> formatting tool は behavior を変えない範囲で使います。学習注釈の追加では output string、API 名、target 名を変更しません。

## Source Paragraph 013

Context: `Code Formatting (pre-commit hooks)`

English paragraph 13:
> The linters used by the CUDA Samples are listed in `.pre-commit-config.yaml`. For example, C++ and CUDA code is
> formatted with [`clang-format`](https://clang.llvm.org/docs/ClangFormat.html).

> **日本語**
> 原文は formatting と lint の自動チェックを説明しています。`.pre-commit-config.yaml`、`clang-format` は coding style をそろえる道具で、CUDA kernel の出力仕様とは別に扱います。
> **学習メモ**
> formatting tool は behavior を変えない範囲で使います。学習注釈の追加では output string、API 名、target 名を変更しません。

## Source Paragraph 014

Context: `Code Formatting (pre-commit hooks)`

English paragraph 14:
> To use `pre-commit`, install via `conda` or `pip`:

> **日本語**
> 原文は formatting と lint の自動チェックを説明しています。`pre-commit`、`conda`、`pip` は coding style をそろえる道具で、CUDA kernel の出力仕様とは別に扱います。
> **学習メモ**
> formatting tool は behavior を変えない範囲で使います。学習注釈の追加では output string、API 名、target 名を変更しません。

## Source Paragraph 015

Context: `Code Formatting (pre-commit hooks)`

English paragraph 15:
> Then run pre-commit hooks before committing code:

> **日本語**
> 原文は formatting と lint の自動チェックを説明しています。`pre-commit` は coding style をそろえる道具で、CUDA kernel の出力仕様とは別に扱います。
> **学習メモ**
> formatting tool は behavior を変えない範囲で使います。学習注釈の追加では output string、API 名、target 名を変更しません。

## Source Paragraph 016

Context: `Code Formatting (pre-commit hooks)`

English paragraph 16:
> By default, pre-commit runs on staged files (only changes and additions that will be committed). To run pre-commit
> checks on all files, execute:

> **日本語**
> 原文は formatting と lint の自動チェックを説明しています。`pre-commit` は coding style をそろえる道具で、CUDA kernel の出力仕様とは別に扱います。
> **学習メモ**
> formatting tool は behavior を変えない範囲で使います。学習注釈の追加では output string、API 名、target 名を変更しません。

## Source Paragraph 017

Context: `Code Formatting (pre-commit hooks)`

English paragraph 17:
> Optionally, you may set up the pre-commit hooks to run automatically when you make a git commit. This can be done by
> running:

> **日本語**
> 原文は formatting と lint の自動チェックを説明しています。`pre-commit` は coding style をそろえる道具で、CUDA kernel の出力仕様とは別に扱います。
> **学習メモ**
> formatting tool は behavior を変えない範囲で使います。学習注釈の追加では output string、API 名、target 名を変更しません。

## Source Paragraph 018

Context: `Code Formatting (pre-commit hooks)`

English paragraph 18:
> Now code linters and formatters will be run each time you commit changes.

> **日本語**
> 原文は formatting と lint の自動チェックを説明しています。主要な語句 は coding style をそろえる道具で、CUDA kernel の出力仕様とは別に扱います。
> **学習メモ**
> formatting tool は behavior を変えない範囲で使います。学習注釈の追加では output string、API 名、target 名を変更しません。

## Source Paragraph 019

Context: `Code Formatting (pre-commit hooks)`

English paragraph 19:
> You can skip these checks with `git commit --no-verify` or with the short version `git commit -n`, althoguh please
> note that this may result in pull requests being rejected if subsequent checks fail.

> **日本語**
> 原文は upstream repository 向けの pull request 手順を説明しています。このローカル学習作業では PR は作成せず、文書上の参照としてだけ読みます。
> **学習メモ**
> この goal の hard rule では PR を作成しません。ここでは原文の意味を理解するだけで、実行手順として扱いません。

## Source Paragraph 020

Context: `Review Process`

English paragraph 20:
> Once submitted, maintainers will be automatically assigned to review the pull request. They might suggest changes or
> improvements. Constructive feedback is a part of the collaborative process, aimed at ensuring the highest quality
> code.

> **日本語**
> 原文は upstream repository 向けの pull request 手順を説明しています。このローカル学習作業では PR は作成せず、文書上の参照としてだけ読みます。
> **学習メモ**
> この goal の hard rule では PR を作成しません。ここでは原文の意味を理解するだけで、実行手順として扱いません。

## Source Paragraph 021

Context: `Review Process`

English paragraph 21:
> For constructive feedback and effective communication during reviews, we recommend following [Conventional
> Comments](https://conventionalcomments.org/).

> **日本語**
> この段落は `Review Process` セクションの contributor 向け手順です。主要な語句 を変えず、作業単位、検証、formatting の目的を確認します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 022

Context: `Review Process`

English paragraph 22:
> Further recommended reading for successful PR reviews:

> **日本語**
> この段落は `Review Process` セクションの contributor 向け手順です。主要な語句 を変えず、作業単位、検証、formatting の目的を確認します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 023

Context: `Review Process`

English paragraph 23:
> - [How to Do Code Reviews Like a Human (Part One)](https://mtlynch.io/human-code-reviews-1/)

> **日本語**
> この段落は `Review Process` セクションの contributor 向け手順です。主要な語句 を変えず、作業単位、検証、formatting の目的を確認します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 024

Context: `Review Process`

English paragraph 24:
> - [How to Do Code Reviews Like a Human (Part Two)](https://mtlynch.io/human-code-reviews-2/)

> **日本語**
> この段落は `Review Process` セクションの contributor 向け手順です。主要な語句 を変えず、作業単位、検証、formatting の目的を確認します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Source Paragraph 025

Context: `Thank You`

English paragraph 25:
> Your contributions enhance the CUDA Samples for the entire community. We appreciate your effort and collaboration!

> **日本語**
> この段落は `Thank You` セクションの contributor 向け手順です。主要な語句 を変えず、作業単位、検証、formatting の目的を確認します。
> **学習メモ**
> この手順文書では command、path、remote、branch、tool 名を英語のまま読みます。このローカル学習作業では output string、API 名、target 名を変更しません。

## Cross References

English anchor: related Japanese study material for this repository.

> **日本語**
> 関連する sample ごとの `README.ja.md`、`docs_ja/themes/`、`docs_ja/glossary/`、source 内の `JP:` コメントを合わせて読むと、本文の build/run 手順と CUDA concept を接続できます。
> **学習メモ**
> この file は文書の伴走資料です。behavior、build graph、test output を変える目的の変更ではありません。

## Verified Source Snippets



??????? source/build/script file ???????????????????



Source: CMakeLists.txt:2-24
```cmake

cmake_minimum_required(VERSION 3.20)

project(cuda-samples LANGUAGES C CXX CUDA)

# JP: `find_package`: この CMake 行で CUDA target、architecture、library dependency を配線します。target 名と link 設定は挙動に直結します。
find_package(CUDAToolkit REQUIRED)

set(CMAKE_POSITION_INDEPENDENT_CODE ON)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

set(CMAKE_CUDA_STANDARD 17)
set(CMAKE_CUDA_STANDARD_REQUIRED ON)

set(CMAKE_CUDA_ARCHITECTURES 75 80 86 87 89 90 100 110 120)
set(CMAKE_CUDA_FLAGS "${CMAKE_CUDA_FLAGS} -Wno-deprecated-gpu-targets")
if(ENABLE_CUDA_DEBUG)
    set(CMAKE_CUDA_FLAGS "${CMAKE_CUDA_FLAGS} -G")        # enable cuda-gdb (may significantly affect performance on some targets)
else()
    set(CMAKE_CUDA_FLAGS "${CMAKE_CUDA_FLAGS} -lineinfo") # add line information to all builds for debug tools (exclusive to -G option)
endif()
```

> JP: ?? CMake ????repository ??? CUDA ??????Toolkit ???architecture/debug flag ?????????build/run ??????????????? sample target ?????????????



Source: run_tests.py:53-73
```python

def load_args_config(config_file):
    """Load arguments configuration from JSON file"""
    if not config_file or not os.path.exists(config_file):
        return {}

    try:
        with open(config_file, 'r') as f:
            config = json.load(f)

        # Validate the config format
        if not isinstance(config, dict):
            print("Warning: Config file must contain a dictionary/object")
            return {}

        return config
    except json.JSONDecodeError:
        print("Warning: Failed to parse config file as JSON")
        return {}
    except Exception as e:
        print(f"Warning: Error reading config file: {str(e)}")
```

> JP: ?? Python ????test_args.json ??????? workflow ??????run/test ????? companion ????? file?error path????????????????????
