# CUDA API Cheat Sheet

English API | ??? | ????
--- | --- | ---
`cudaGetDeviceCount` | ???? GPU ????? | sample ? skip ?????????
`cudaSetDevice` | ??? GPU ??? | ??? allocation ? launch ???????????
`cudaMalloc` | device memory ??? | `cudaFree` ???????
`cudaMallocHost` / `cudaHostAlloc` | pinned host memory ??? | async copy ? overlap ??????????????
`cudaMallocManaged` | managed memory ??? | pointer ?????????????
`cudaMemcpy` | ????????? | ???? enum ????????
`cudaMemcpyAsync` | stream ????????? | pinned memory ? stream ?????????
`cudaMemset` / `cudaMemsetAsync` | device memory ??? | byte ????????????????
`cudaStreamCreate` | stream ?? | work ??? lane ??????
`cudaEventRecord` | event ?? | timing ? dependency ??????
`cudaDeviceSynchronize` | device ??????? | host ???????????????
`cudaGetLastError` | ?? launch error ?? | ?????? error check ?????????
`cuInit` | Driver API ??? | Driver API ??????????
`cuModuleLoad` / `cuModuleLoadData` | module ???? | PTX/CUBIN/NVRTC ????????????
`cuLaunchKernel` | Driver API kernel launch | Runtime ? `<<<...>>>` ??????????????
`nvrtcCompileProgram` | runtime compile | JIT ????? compile log ? option ???????

> **???**
> API ????????????????????????????????????????????
>
> **????**
> CUDA Runtime API ? `cuda`?Driver API ? `cu`?NVRTC ? `nvrtc` ?????????????
