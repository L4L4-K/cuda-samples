# Unified Memory

English anchor: Unified Memory provides one allocation visible to CPU and GPU with migration managed by CUDA.

> **???**
> Unified Memory ? `cudaMallocManaged` ?????CPU ? GPU ??????? pointer ?????????????????? processor ?????????????????????????????
>
> **????**
> pointer ?????????????????????????????????

## Reading Patterns

- Initialization may happen on CPU, then kernel touches the same allocation on GPU.
- `cudaDeviceSynchronize` is often needed before CPU reads GPU-written managed data.
- Prefetch and memory advice samples expose migration control.
- Multi-GPU managed memory depends on device support and access patterns.

> **???**
> managed memory ????????????????????????????????????????????????????????
>
> **????**
> page fault ??????prefetch ????????????????????

## Common Mistakes

- Assuming `cudaMallocManaged` removes synchronization needs.
- Measuring first-touch migration as if it were kernel compute time.
- Accessing managed data from CPU while GPU work is still active.
- Forgetting that oversubscription changes performance behavior.

> **???**
> Unified Memory ???????????????????????????????????????
>
> **????**
> ?????????????? migration ???????????


## Reading Checklist

- Identify which side owns each allocation: host, device, managed, pinned, mapped, or external.
- Find the first transfer or mapping that makes data visible to the GPU.
- Find the kernel launch or library call that performs device work.
- Find the synchronization point that makes results safe to read on the host.
- Ask whether the sample is teaching correctness, interoperability, or performance.

> **???**
> CUDA ??????????????????????????????????????????
>
> **????**
> `cudaMemcpy` ? `cudaDeviceSynchronize` ??????????????????????????????????????????
