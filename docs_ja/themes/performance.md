# Performance

English anchor: CUDA performance depends on memory traffic, occupancy, launch overhead, instruction mix, synchronization, and library choices.

> **???**
> CUDA ???? kernel ????????????????????coalescing?occupancy?launch overhead????????????????????????????
>
> **????**
> ?????????????????? transfer ??????kernel ????????????

## Sample Signals

- Timing with CUDA events usually measures GPU-side elapsed time.
- Timing with CPU timers may include launch overhead or synchronization.
- Occupancy samples expose active warps/blocks but not full performance by themselves.
- Transpose, reduction, convolution, and matrix samples often highlight memory access shape.

> **???**
> occupancy ?????????????????????????????????memory-bound ? compute-bound ??????????
>
> **????**
> ??????????????????????????????????

## Practical Questions

- Are global memory accesses coalesced?
- Is data reused through shared memory, caches, texture, or library plans?
- Are transfers overlapped with compute?
- Is launch overhead large relative to the work size?
- Is the sample comparing against a CPU or reference implementation fairly?

> **???**
> ?????????????? CPU ??? GPU ???????????CPU ???????????????????????
>
> **????**
> `PASS` ????????????????????????????????????


## Reading Checklist

- Identify which side owns each allocation: host, device, managed, pinned, mapped, or external.
- Find the first transfer or mapping that makes data visible to the GPU.
- Find the kernel launch or library call that performs device work.
- Find the synchronization point that makes results safe to read on the host.
- Ask whether the sample is teaching correctness, interoperability, or performance.

> **???**
> CUDA ??????????????????????????????????????????
>
> **????**
> `cudaMemcpy` ? `cudaDeviceSynchronize` ??????????????????????????????????????????
