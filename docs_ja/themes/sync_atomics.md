# Synchronization And Atomics

English anchor: CUDA synchronization controls visibility and ordering; atomics protect shared updates.

> **???**
> CUDA ?????? thread ???????????????????????????????????atomics ??? thread ?????????????????????
>
> **????**
> ???????????? address ??? thread ?????????????

## Device-Side Synchronization

- `__syncthreads()` synchronizes threads within one block.
- Cooperative groups can synchronize a named group with clearer scope.
- Memory fences order writes but do not necessarily wait for all threads.
- Warp-level intrinsics assume a well-defined participating mask.

> **???**
> `__syncthreads()` ? block ??????grid ?????????? kernel ?????cooperative launch ????host ??????????????????
>
> **????**
> shared memory ??? reduction ????????? `__syncthreads()` ????? thread ??????

## Atomics

- Atomics serialize updates to the same memory location enough to avoid lost updates.
- Atomics can be expensive under high contention.
- Warp aggregation and block-level reductions reduce the number of global atomics.
- System-scope atomics matter when CPU or peer devices observe the same memory.

> **???**
> atomic ????????????? address ????????????????????????????????? atomic ?????????
>
> **????**
> atomic ??????????????????????????????????????


## Reading Checklist

- Identify which side owns each allocation: host, device, managed, pinned, mapped, or external.
- Find the first transfer or mapping that makes data visible to the GPU.
- Find the kernel launch or library call that performs device work.
- Find the synchronization point that makes results safe to read on the host.
- Ask whether the sample is teaching correctness, interoperability, or performance.

> **???**
> CUDA ??????????????????????????????????????????
>
> **????**
> `cudaMemcpy` ? `cudaDeviceSynchronize` ??????????????????????????????????????????
