# Shared Memory

English anchor: shared memory is programmer-managed memory shared by threads in a block.

> **???**
> shared memory ? block ? thread ?????????????????global memory ??????????????????block ? reduction ?????????????
>
> **????**
> `__shared__` ?????????????????`__syncthreads()` ??????????????

## Typical Uses

- Matrix multiplication tiles.
- Stencil and convolution neighborhoods.
- Reductions and scans.
- Transpose buffers that improve coalescing.

> **???**
> tile ??global memory ???????? shared memory ?????????????????????
>
> **????**
> shared memory ? block ???????????? block ? shared memory ?????????

## Performance Notes

- Reuse can reduce global memory traffic.
- Bank conflicts can serialize shared memory access.
- More shared memory per block can reduce occupancy.
- Dynamic shared memory appears in the third kernel launch parameter.

> **???**
> shared memory ?????????????????????global memory traffic?bank conflict?occupancy ??????????
>
> **????**
> `extern __shared__` ? launch ????????? shared memory ???


## Reading Checklist

- Identify which side owns each allocation: host, device, managed, pinned, mapped, or external.
- Find the first transfer or mapping that makes data visible to the GPU.
- Find the kernel launch or library call that performs device work.
- Find the synchronization point that makes results safe to read on the host.
- Ask whether the sample is teaching correctness, interoperability, or performance.

> **???**
> CUDA ??????????????????????????????????????????
>
> **????**
> `cudaMemcpy` ? `cudaDeviceSynchronize` ??????????????????????????????????????????
