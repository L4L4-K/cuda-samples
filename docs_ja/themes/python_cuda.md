# Python CUDA

English anchor: Python samples use CUDA from Python libraries while preserving the same host/device concepts.

> **???**
> Python CUDA ?????????? C++ ?????????????GPU ?????kernel/library ????????????????CuPy?Numba?cuda-python?PyTorch?TensorFlow ??? API ?????????
>
> **????**
> Python ???????????????? device allocation ? stream work ???????????????

## What To Track

- Which library owns the array or tensor.
- Whether data is NumPy CPU data, CuPy GPU data, framework tensor, or CUDA buffer.
- Where synchronization is implicit or explicit.
- Whether custom kernels are JIT compiled or loaded from extension code.

> **???**
> `numpy` ??? GPU ???????????????????????????`asnumpy` ? `.get()` ? GPU ?? CPU ???????????
>
> **????**
> Python ? benchmark ???? JIT?lazy execution?implicit synchronization ??????????????

## Framework Interop

- Custom PyTorch and TensorFlow samples bridge framework tensors and CUDA kernels.
- Lifetime is often owned by the framework, not raw CUDA code.
- Streams may be framework-managed.
- Build steps can require compiler, CUDA headers, and Python package versions.

> **???**
> framework interop ???CUDA pointer ??????????allocation owner ? framework ???????????
>
> **????**
> tensor ? device?dtype?contiguity?stream ??????? custom kernel ?????????


## Reading Checklist

- Identify which side owns each allocation: host, device, managed, pinned, mapped, or external.
- Find the first transfer or mapping that makes data visible to the GPU.
- Find the kernel launch or library call that performs device work.
- Find the synchronization point that makes results safe to read on the host.
- Ask whether the sample is teaching correctness, interoperability, or performance.

> **???**
> CUDA ??????????????????????????????????????????
>
> **????**
> `cudaMemcpy` ? `cudaDeviceSynchronize` ??????????????????????????????????????????
